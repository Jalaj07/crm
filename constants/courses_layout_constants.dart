// lib/constants/layout_constants.dart

// Note: No Flutter import needed if only defining primitive types like double.
// import 'package:flutter/material.dart'; // Add if using EdgeInsets, Color, etc.

const double kCardPadding = 16.0;
const double kVerticalSpacerSmall = 4.0;
const double kVerticalSpacerMedium = 8.0;
const double kVerticalSpacerLarge = 16.0;
const double kListItemSpacing = 16.0; // Spacing between course cards
const double kPagePadding = 16.0; // Padding around the list view
const double kBorderRadius = 8.0;
