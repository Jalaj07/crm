import 'package:flutter/material.dart';

class FfmTeamExpensesScreen extends StatelessWidget {
  const FfmTeamExpensesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('FFM Team Expenses Screen'));
  }
}